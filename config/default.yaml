seed: 42
device: cpu

env:
  n_agents: 3
  obs_dim: 8
  action_dim: 2
  max_steps: 50

train:
  num_episodes: 10
  batch_size: 32
  buffer_size: 100000
  actor_lr: 0.001
  critic_lr: 0.001
  gamma: 0.99
  tau: 0.005
  hidden_dim: 128

eval:
  episodes: 3
